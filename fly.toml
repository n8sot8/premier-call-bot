app = "premier-call-bot"
primary_region = "nrt"

kill_signal = "SIGINT"
kill_timeout = "5s"

[build]
  dockerfile = "Dockerfile"

[env]
  TZ = "Asia/Tokyo"
  DB_PATH = "/data/bot.db"

[processes]
  app = "python bot.py"

[mounts]
  source = "data"
  destination = "/data"
